interface Staff {
    _id: ObjectId;
    staff_name: string;
    email: string;
    password: string;
    phone: string;
    isActive: boolean; 
    role: number; // Vai trò của nhân viên (Admin, Manager)
    permissions: string[]; // Mảng quyền truy cập cụ thể
}